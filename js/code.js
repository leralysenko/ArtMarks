$(document).ready(function(){
 
   $('.jRating').jRating({
     step : true, // без шага
     length : 5, // показать 10 звезд при инициализации
     type : 'small' // показать маленькие звезды, а не большие по умолчанию
   });
});
